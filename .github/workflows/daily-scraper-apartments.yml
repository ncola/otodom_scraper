name: Daily Otodom full scraper (apartments)

on:
  schedule:
    - cron: "0 3 * * *"
  workflow_dispatch:
    inputs:
      city:
        description: "Ad-hoc single city (leave empty to run the scheduled matrix)"
        required: false
        default: ""

jobs:
  scrape:
    runs-on: ubuntu-latest
    # dispatch with a city -> run just that one; scheduled or dispatch without input -> run the default matrix
    strategy:
      fail-fast: false
      matrix:
        city: ${{ fromJSON(inputs.city && format('["{0}"]', inputs.city) || '["katowice"]') }}

    steps:
      - uses: actions/checkout@v4

      - name: Create .env
        run: |
          cat > .env <<EOF
          DB_HOST=${{ secrets.DB_HOST }}
          DB_PORT=${{ secrets.DB_PORT }}
          DB_NAME=${{ secrets.DB_NAME }}
          DB_USER=${{ secrets.DB_USER }}
          DB_PASSWORD=${{ secrets.DB_PASSWORD }}
          DB_SSLMODE=${{ secrets.DB_SSLMODE }}
          CITY=${{ matrix.city }}
          EOF

      - name: Run scraper
        run: docker compose -f docker-compose.github.yml up --build --abort-on-container-exit scraper_full

      - name: Upload logs
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: scraper-logs-${{ matrix.city }}
          path: logs/
